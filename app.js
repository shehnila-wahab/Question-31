var user = ['sana', 'sahil', 'saba', 'shehnila', 'anam'];
if (user.length) {
    console.log("We need to find some users!");
}
else {
    user = [];
    console.log("all the users have been removed" + user.length);
}
